<!--
SKETCH — written by `border-collie init` (v0.6.0-sketch).

`init` found an existing AGENTS.md and did not touch it. This file exists only
because Claude Code reads CLAUDE.md, and points at what was already here rather
than forking a second set of instructions that would drift.

Open question for the real `init`: is this pointer right, or should `init`
detect AGENTS.md and write nothing at all? See SKETCH.md.
-->

# acfirst-web

See [AGENTS.md](./AGENTS.md) for commands and code style — it is the source of
truth for both.

See [WORKFLOW.md](./WORKFLOW.md) for how border-collie runs work here: the
workspace hooks, and the named commands that gate a merge.

Domain language and architecture decisions: [CONTEXT.md](./CONTEXT.md) and
[docs/adr/](./docs/adr/).
